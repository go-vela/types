secrets:
  - origin:
      name: vault secrets
      image: target/vela/secret-vault:${VELA_BUILD_COMMIT:0:7}
      parameters:
        items:
          - source: secret/vela/dev/docker
            path: docker